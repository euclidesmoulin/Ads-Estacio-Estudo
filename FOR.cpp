  // (iniciliação; condição; incremento)  
    //{código}
    //for ( ini;cond/inc)
    // { cod }

#include <stdio.h>

    int main() {
  // (iniciliação; condição; incremento)
        for (int i=1; i<=6; i++){
            printf("%d\n", i);
        }

        return 0;
        
    }